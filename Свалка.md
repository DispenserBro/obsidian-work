
```python
for i in range(1, 10):
	print(f"cycle number: {i}")
vault_name = @vault_path.split("/")[-1]
print(f'Vault name: {vault_name}')
```

```csharp
Console.WriteLine("Hello");
```


> [!tip]- Привет
> Как дела?

<!--
---- файл "Общий чейнджлог"

```dataview
LIST WITHOUT ID L.text AS Логи FROM "Логи/CHANGELOG_2025-09-17" FLATTEN file.lists AS L WHERE meta(L.section).subpath = "Added" AND L.text != ""
```

```dataview
LIST WITHOUT ID L.text FROM "Логи/CHANGELOG_2025-09-17" FLATTEN file.lists AS L WHERE meta(L.section).subpath = "Added" AND L.text != ""
```

```dataview
LIST WITHOUT ID L.text FROM #logs FLATTEN file.lists AS L WHERE meta(L.section).subpath = "Added" AND L.text != "" SORT file.ctime DESC
```
```dataview
TABLE WITHOUT ID D AS Дата, L.text AS Логи FROM #logs FLATTEN file.lists AS L FLATTEN dateformat(date(split(file.name, "_")[1]), "dd-MM") AS D WHERE meta(L.section).subpath = "Added" AND L.text != "" AND D > "01-09"
```

```dataview
LIST WITHOUT ID L.text FROM #logs FLATTEN file.lists AS L FLATTEN dateformat(date(split(file.name, "_")[1]), "dd-MM") AS D WHERE meta(L.section).subpath = "Added" AND L.text != "" AND D > "01-09"
```

```dataview
TABLE WITHOUT ID D AS Дата, L.text AS Логи FROM #logs FLATTEN file.lists as L FLATTEN dateformat(date(split(file.name, "_")[1]), "dd-MM") AS D WHERE meta(L.section).subpath = "Added" AND L.text != "" AND D > "01-09"
SORT D
```
```dataview
TABLE rows.L.text AS "Логи"
FROM #logs
FLATTEN file.lists AS L
FLATTEN date(split(file.name, "_")[1]) AS dval
WHERE meta(L.section).subpath = "Added"
  AND L.text != ""
  AND dateformat(dval, "dd-MM") > "01-09"
GROUP BY dateformat(dval, "dd-MM") AS "Дата"
```


```dataview
```

----
---- файл "Шаблоны"

`= TABLE WITHOUT ID file.link AS Ссылка, description AS Описание FROM "Шаблоны" WHERE file.name != "Шаблоны"`
```dataview
TABLE WITHOUT ID file.link AS Ссылка, dateformat(file.ctime, "DD") AS "Дата создания" FROM "Шаблоны"
```

----
---- файл "Индекс заметок"

```dataviewjs
	// dv.list(dv.pages('"Ежедневные заметки"').sort(f => f.file.сtime.ts, "desc").file.link)
	for (let group of dv.pages('"Ежедневные заметки"').groupBy(
	p => p.file.name.split("-")[1])) {
		dv.header(3, moment(group.key).format("MMMM")); dv.list(group.rows.sort(
			f => f.file.сtime, 'desc').file.link)
	}
``` 

```dataview
LIST rows.file.link
FROM "Ежедневные заметки"
SORT file.ctime DESC
GROUP BY dateformat(date(file.name), "MMM")
```

----
-->
